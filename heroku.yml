setup:
  addons:
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: backend/Dockerfile
run:
  web: /start.sh
  worker:
    command:
      - celery -A celery app/worker --concurrency=1 -l INFO --pidfile=
    image: web
